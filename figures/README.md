# figures/

汇报里用到的图。`crop_results_bar.png`（五数据集三种输入）、`nabirds_size_bins.png`（按主体大小分组）、`cub_res224_bar.png`（224 分辨率探针）、`token_select.png`（token 选择曲线）四张由 `results/` 里的数字画出；`nabirds_boxes.jpg`（自动框 vs 官方框抽样）、`nabirds_cases.jpg`（原图错、裁剪对的案例）由 `locate/viz_boxes.py`、`locate/nabirds_cases.py` 生成；`pca_v2_vs_v3.jpg`、`bg_quant_n1000.png`、`evidence_heatmaps.jpg`、`fgvc_bar.png`、`variants.png` 来自 Mac 前置实验（`experiments/outputs/`）。
